<%= form_for(@user_project_participation) do |f| %>
  <% if @user_project_participation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_project_participation.errors.count, "error") %> prohibited this user_project_participation from being saved:</h2>

      <ul>
      <% @user_project_participation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.text_field :project_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
